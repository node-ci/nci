
plugins:
    # - nci-mail-notification

nodes:
    - type: local
      maxExecutorsCount: 2

http:
    host: 127.0.0.1
    port: 3000

storage:
    backend: memdown
    # backend: medeadown

notify:
    mail:
        host: smtp.gmail.com
        port: 587
        auth:
            user: bot.nci@gmail.com
            pass: pass
