
scm:
    type: git
    repository: https://github.com/node-ci/nci
    rev: master

# catchRev:
#     comment: !!js/regexp //

notify:
    on:
        - error
    to:
        console:

steps:
  - name: sync
    cmd: npm install && npm prune

  - name: test
    cmd: npm test
