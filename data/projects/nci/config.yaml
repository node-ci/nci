
scm:
    type: git
    repository: ./
    rev: master

# catchRev:
#     comment: !!js/regexp //

notify:
    on:
        - error
    to:
        console:

steps:
    - cmd: npm install && npm prune
    - cmd: npm test
